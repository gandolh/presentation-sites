---
// A single post, rendered in claymorphism.
import type { Post } from "../../content/posts";
import Shell from "../../themes/claymorphism/Shell.astro";
import PostView from "../../themes/claymorphism/Post.astro";
import { postPaths, postPage } from "../../lib/routes";

export const getStaticPaths = postPaths;

interface Props {
  post: Post;
}

const { post } = Astro.props;
const { style, title, description } = postPage("claymorphism", post);
---

<Shell style={style} title={title} description={description} postSlug={post.slug}>
  <PostView post={post} />
</Shell>
