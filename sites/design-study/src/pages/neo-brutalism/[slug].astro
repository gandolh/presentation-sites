---
// A single post, rendered in neo-brutalism.
import type { Post } from "../../content/posts";
import Shell from "../../themes/neo-brutalism/Shell.astro";
import PostView from "../../themes/neo-brutalism/Post.astro";
import { postPaths, postPage } from "../../lib/routes";

export const getStaticPaths = postPaths;

interface Props {
  post: Post;
}

const { post } = Astro.props;
const { style, title, description } = postPage("neo-brutalism", post);
---

<Shell style={style} title={title} description={description} postSlug={post.slug}>
  <PostView post={post} />
</Shell>
