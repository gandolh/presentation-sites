---
// A single post, rendered in liquid-glass.
import type { Post } from "../../content/posts";
import Shell from "../../themes/liquid-glass/Shell.astro";
import PostView from "../../themes/liquid-glass/Post.astro";
import { postPaths, postPage } from "../../lib/routes";

export const getStaticPaths = postPaths;

interface Props {
  post: Post;
}

const { post } = Astro.props;
const { style, title, description } = postPage("liquid-glass", post);
---

<Shell style={style} title={title} description={description} postSlug={post.slug}>
  <PostView post={post} />
</Shell>
