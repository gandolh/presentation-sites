---
// A single post, rendered in ethereal.
import type { Post } from "../../content/posts";
import Shell from "../../themes/ethereal/Shell.astro";
import PostView from "../../themes/ethereal/Post.astro";
import { postPaths, postPage } from "../../lib/routes";

export const getStaticPaths = postPaths;

interface Props {
  post: Post;
}

const { post } = Astro.props;
const { style, title, description } = postPage("ethereal", post);
---

<Shell style={style} title={title} description={description} postSlug={post.slug}>
  <PostView post={post} />
</Shell>
