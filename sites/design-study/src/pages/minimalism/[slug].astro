---
// A single post, rendered in minimalism.
import type { Post } from "../../content/posts";
import Shell from "../../themes/minimalism/Shell.astro";
import PostView from "../../themes/minimalism/Post.astro";
import { postPaths, postPage } from "../../lib/routes";

export const getStaticPaths = postPaths;

interface Props {
  post: Post;
}

const { post } = Astro.props;
const { style, title, description } = postPage("minimalism", post);
---

<Shell style={style} title={title} description={description} postSlug={post.slug}>
  <PostView post={post} />
</Shell>
