---
import Document from "../../layouts/Document.astro";
import { site } from "../../content/site";
import { withBase } from "@sites/kit";
import type { Style } from "../../content/styles";
import { meta } from "./meta";

import "@fontsource-variable/inter/wght.css";
import "./theme.css";

interface Props {
  style: Style;
  title: string;
  description?: string;
  postSlug?: string;
}

const { style, title, description, postSlug } = Astro.props;

// The wordmark is this page's <h1> on a feed; on a post the article
// title takes that role and the wordmark steps down to a <p>.
const MarkTag = postSlug ? "p" : "h1";
---

<Document
  slug={style.slug}
  title={title}
  description={description}
  postSlug={postSlug}
  themeColor={meta.themeColor}
  scheme={meta.scheme}
>
  <div class="ds-page">
    <div class="gl-wrap">
      <header class="gl-head gl-pane">
        <MarkTag class="gl-head__mark"><a href={withBase("/glassmorphism/")}>{site.name}</a></MarkTag>
        <p class="gl-head__style">{style.name} &middot; {style.era}</p>
        <a class="gl-chip" href={withBase("/")}>All 14 styles</a>
      </header>

      <main>
        <slot />
      </main>

      <footer class="gl-foot gl-pane">
        <p>{site.name} &mdash; everything is a pane, nothing is a box.</p>
      </footer>
    </div>
  </div>
</Document>
