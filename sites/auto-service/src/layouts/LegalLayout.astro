---
import Base from "./Base.astro";
import Footer from "../components/Footer.astro";
import { site } from "../content/site";
import { withBase } from "@sites/kit";

interface Props {
  title: string;
  description?: string;
  updated?: string;
}

const {
  title,
  description = `${title} · ${site.name}`,
  updated = site.legal.documentsUpdated,
} = Astro.props;
---

<Base title={`${title} · ${site.name}`} description={description}>
  <header class="border-b border-[var(--color-outline-variant)] bg-[var(--color-background)] text-[var(--color-on-surface)]">
    <div class="container-page flex items-center justify-between py-5">
      <a href={withBase("/")} class="wordmark text-xl md:text-2xl uppercase">{site.name}</a>
      <a
        href={withBase("/")}
        class="text-sm font-semibold text-[var(--color-on-surface-variant)] hover:text-[var(--color-on-surface)] transition-colors"
      >
        ← Înapoi la site
      </a>
    </div>
  </header>

  <main class="container-page max-w-3xl py-12 md:py-20">
    <h1 class="heading-lg">{title}</h1>
    <span class="m-stripe mt-4"></span>
    <p class="mt-3 text-sm text-[var(--color-on-surface-variant)]">
      Ultima actualizare: {updated}
    </p>

    <article class="legal-prose mt-10">
      <slot />
    </article>
  </main>

  <Footer />
</Base>

<style is:global>
  .legal-prose {
    color: var(--color-on-surface-variant);
    line-height: 1.7;
    font-size: 1rem;
  }
  .legal-prose h2 {
    font-family: var(--font-display);
    font-weight: 750;
    font-size: clamp(1.3rem, 3vw, 1.6rem);
    letter-spacing: -0.02em;
    color: var(--color-on-surface);
    margin-top: 2.5rem;
    margin-bottom: 0.75rem;
    line-height: 1.2;
  }
  .legal-prose h3 {
    font-family: var(--font-body);
    font-weight: 700;
    font-size: 1.05rem;
    color: var(--color-on-surface);
    margin-top: 1.75rem;
    margin-bottom: 0.5rem;
  }
  .legal-prose p {
    margin-bottom: 1rem;
  }
  .legal-prose ul,
  .legal-prose ol {
    margin: 0.5rem 0 1.25rem;
    padding-left: 1.5rem;
    list-style: disc;
  }
  .legal-prose ol {
    list-style: decimal;
  }
  .legal-prose li {
    margin-bottom: 0.5rem;
  }
  .legal-prose a {
    color: var(--color-primary-bright);
    text-decoration: underline;
    text-underline-offset: 2px;
    font-weight: 600;
  }
  .legal-prose a:hover {
    color: var(--color-on-surface);
  }
  .legal-prose strong {
    color: var(--color-on-surface);
    font-weight: 700;
  }
  .legal-prose hr {
    border: none;
    border-top: 1px solid var(--color-outline-variant);
    margin: 2rem 0;
  }
  .legal-prose .note {
    background-color: var(--color-surface-container-low);
    border: 1px solid var(--color-outline-variant);
    border-radius: var(--radius-lg);
    padding: 1rem 1.25rem;
    margin: 1.5rem 0;
    font-size: 0.95rem;
    color: var(--color-on-surface-variant);
  }
  .legal-prose table {
    width: 100%;
    border-collapse: collapse;
    margin: 1rem 0 1.5rem;
    font-size: 0.95rem;
  }
  .legal-prose th,
  .legal-prose td {
    text-align: left;
    padding: 0.6rem 0.75rem;
    border-bottom: 1px solid var(--color-outline-variant);
    vertical-align: top;
  }
  .legal-prose th {
    color: var(--color-on-surface);
    font-weight: 700;
    font-family: var(--font-body);
  }
</style>
