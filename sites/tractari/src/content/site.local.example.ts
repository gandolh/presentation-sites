// Copy to `site.local.ts` (git-ignored) and fill in REAL values to turn this
// demo into a live site. Every field is optional — only specify overrides.
// Deep-merged over the public defaults in `site.ts` at build time.
//
// import type { SiteOverrides } from "./site";

export const siteOverrides /*: SiteOverrides */ = {
  // name: "Numele Real S.R.L.",
  // phones: [
  //   { label: "Ionuț P.", display: "0720 000 000", e164: "40720000000" },
  // ],
  // email: "contact@domeniu.ro",
  // demo: { show: false }, // hide the demo banner once real
  // legal: { legalName: "...", cui: "RO12345678", regNumber: "J18/123/2026" },
};
