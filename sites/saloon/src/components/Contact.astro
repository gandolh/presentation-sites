---
import { site, waLink } from "../content/site";
---

<section id="contact" class="section">
  <div class="container-page">
    <div class="reveal max-w-2xl">
      <h2 class="heading-lg">Contact</h2>
      <span class="gold-divider"></span>
    </div>

    <div class="mt-12 grid md:grid-cols-2 gap-10 md:gap-16">
      <div class="reveal space-y-8">
        <div>
          <p class="label-md uppercase text-[var(--color-on-surface-variant)]">
            Telefon · WhatsApp
          </p>
          <a
            href={`tel:+${site.phoneE164}`}
            class="mt-2 inline-block font-display text-xl text-[var(--color-on-surface)] hover:text-[var(--color-blush-deep)]"
          >
            {site.phone}
          </a>
        </div>

        <div>
          <p class="label-md uppercase text-[var(--color-on-surface-variant)]">
            Zonă
          </p>
          <p
            class="mt-2 font-display text-xl text-[var(--color-on-surface)] leading-snug"
          >
            {site.city}, {site.county}
          </p>
          <p class="mt-2 text-sm text-[var(--color-on-surface-variant)] max-w-xs">
            Îți comunic adresa exactă pe WhatsApp când stabilim programarea.
          </p>
        </div>

        <div>
          <p class="label-md uppercase text-[var(--color-on-surface-variant)]">
            Email
          </p>
          <a
            href={`mailto:${site.email}`}
            class="mt-2 inline-block font-display text-xl text-[var(--color-on-surface)] hover:text-[var(--color-blush-deep)]"
          >
            {site.email}
          </a>
        </div>

        <div>
          <p class="label-md uppercase text-[var(--color-on-surface-variant)]">
            Program
          </p>
          <ul
            class="mt-3 space-y-2 divide-y divide-[var(--color-outline-variant)]"
          >
            {
              site.hours.map((h) => (
                <li class="flex justify-between gap-4 pt-2 first:pt-0">
                  <span class="text-[var(--color-on-surface-variant)]">
                    {h.day}
                  </span>
                  <span class="text-[var(--color-on-surface)] font-semibold">
                    {h.value}
                  </span>
                </li>
              ))
            }
          </ul>
        </div>
      </div>

      <div class="reveal flex flex-col justify-center gap-6">
        <p class="text-[var(--color-on-surface-variant)] leading-relaxed">
          Cel mai simplu mod de a-ți face o programare este pe WhatsApp: îmi
          scrii ce serviciu te interesează și stabilim împreună ziua și ora.
        </p>
        <div class="flex flex-wrap gap-4">
          <a
            href={waLink()}
            target="_blank"
            rel="noopener"
            class="btn btn-whatsapp"
          >
            Scrie-mi pe WhatsApp
          </a>
          <a href={`tel:+${site.phoneE164}`} class="btn btn-secondary">
            Sună-mă
          </a>
        </div>
      </div>
    </div>
  </div>
</section>
