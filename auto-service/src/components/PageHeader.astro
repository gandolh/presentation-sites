---
// Compact dark page header for subpages (which have no hero). Carries the page
// <h1> and sits directly under the solid nav, echoing the hero's graphite-navy
// surface and M-stripe so the brand reads continuous across routes.
interface Props {
  eyebrow: string;
  title: string;
  lead?: string;
}

const { eyebrow, title, lead } = Astro.props;
---

<section class="bg-[var(--color-background)] text-[var(--color-on-surface)]">
  <div class="container-page pt-12 pb-12 md:pt-16 md:pb-16">
    <div class="reveal flex items-center gap-3">
      <span class="m-stripe" aria-hidden="true"></span>
      <span class="kicker">{eyebrow}</span>
    </div>
    <h1 class="reveal heading-xl mt-4 !text-[clamp(2rem,5vw,3.25rem)] text-[var(--color-on-surface)]">
      {title}
    </h1>
    {
      lead && (
        <p class="reveal mt-4 max-w-2xl text-lg leading-relaxed text-[var(--color-on-surface-variant)]">
          {lead}
        </p>
      )
    }
  </div>
</section>
